const authController = require("./auth");
const tripsController = require("./tripController");
const productsController = require("./productController");

module.exports = {
  authController,
  tripsController,
  productsController,
};
